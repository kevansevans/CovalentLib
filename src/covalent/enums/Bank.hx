package enums;

/**
 * @author Kaelan
 * 
 * constants so I don't have to remember what bank is what
 */
enum abstract Bank(Int) from Int to Int
{
	var OVERWORLD_SPRITES_ONE:Int = 0x30;
	var OVERWORLD_SPRITES_TWO:Int = 0x31;
}